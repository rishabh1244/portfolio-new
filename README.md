# Ri Portfolio — React/Vite

A close React recreation of the supplied redesign reference. Images are intentionally left with blank `src` attributes so you can drop your own assets in later.

## Run

```bash
npm install
npm run dev
```

Then open the Vite URL shown in the terminal (usually `http://localhost:5173`).

## Build

```bash
npm run build
npm run preview
```
